name: askug_dev_config_model
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  colorize: ^3.0.0
#  path: ^1.7.0

dev_dependencies:
#  pedantic: ^1.9.0
  test: ^1.14.4
  logging: ^1.0.1
  lint: ^1.0.0
